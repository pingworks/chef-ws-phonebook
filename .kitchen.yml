---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[ws-phonebook::default]
    attributes:
      ws-base:
        cname: 'test'
        domain: 'pingworks.ws.pingworks.net'
        dns: '10.33.0.10'
